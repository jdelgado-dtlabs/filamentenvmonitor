# FilamentBox Environment Variables
# Copy this file to .env and fill in your actual credentials.
# The .env file is in .gitignore and will NOT be committed to git.

# InfluxDB Credentials (REQUIRED for production)
INFLUXDB_USERNAME=admin
INFLUXDB_PASSWORD=your_secure_password_here
INFLUXDB_DATABASE=influx

# Optional: Override InfluxDB host/port
# INFLUXDB_HOST=influxdb
# INFLUXDB_PORT=8086

# Optional: Measurement name and tags for InfluxDB data points
# These can be set here or in config.yaml. Tags are optional and can be omitted.
# To enable tags, uncomment and customize (tags are JSON-formatted key-value pairs):
# DATA_COLLECTION_MEASUREMENT=environment
# DATA_COLLECTION_TAGS={"location": "filamentbox", "device": "pi-zero-1", "region": "warehouse"}